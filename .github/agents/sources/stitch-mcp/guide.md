# Stitch MCP

Validation fixture for header-authenticated MCP connectivity in CLI integration tests.

## Scope

- Exercise credential storage and MCP connection paths
- Used by `source_test` during validator runs

## Guidelines

- Treat this as read-only test infrastructure
- Keep prompts deterministic when validating the source
